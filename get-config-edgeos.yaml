---
- hosts: 10.0.0.1
  gather_facts: false
  ansible_network_os: edgeos
  connection: network_cli

  collections:
    - community.network

  tasks:
    - name: Gather facts from the device.
      routeros_facts:
         gather_subset: all
